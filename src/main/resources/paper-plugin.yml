name: VanillaPlus
version: '${project.version}'
api-version: '1.21'

main: ch.andre601.vanillaplus.VanillaPlus

dependencies: 
  server: 
    ItemsAdder:
      load: BEFORE
      required: false
    WorldGuard:
      load: BEFORE
      required: false
    IAWAILA:
      load: BEFORE
      required: false
    PlaceholderAPI:
      load: BEFORE
      required: false